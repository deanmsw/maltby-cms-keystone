// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id    String @id @default(cuid())
  name  String @default("")
  email String @unique @default("")
}

model Team {
  id               String   @id @default(cuid())
  name             String   @default("")
  players          Player[] @relation("Team_players")
  league           String   @default("")
  from_Player_team Player[] @relation("Player_team")
}

model Player {
  id                String  @id @default(cuid())
  name              String  @default("")
  email             String  @unique @default("")
  position          String  @default("")
  age               String  @default("")
  previousClub      String  @default("")
  team              Team?   @relation("Player_team", fields: [teamId], references: [id])
  teamId            String? @map("team")
  from_Team_players Team[]  @relation("Team_players")

  @@index([teamId])
}

model Page {
  id              String @id @default(cuid())
  name            String @default("")
  slug            String @default("")
  metaTitle       String @default("")
  metaDescription String @default("")
}

model Sponsor {
  id    String @id @default(cuid())
  title String @default("")
  link  String @default("")
}
